<div class="container">
  <div class="row">
    <h3>Focal Slot Prerequisites</h3>
    <%= form_for (@newFocal), url: {action: "create"} do |f| %>
        <p>Content:</p>
        <%= f.text_field :content %>
        <%= f.submit 'Create', :class => 'button-xs' %>
    <% end %>
    <div class="column">
      <table class="table">
        <tbody>
          <% @focals.each do |focal| %>
            <tr>
              <%= render focal %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
